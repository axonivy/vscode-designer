name: AI-build
on:
  schedule:
    - cron: '0 1 * * 0' # 1 AM on Sunday
  push:
    branches:
      - ai.tests
  workflow_dispatch:

permissions:
  contents: read
  copilot-requests: write

jobs:
  copilot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Setup Java JDK
        uses: actions/setup-java@v5
        with:
          java-version: 25
          distribution: temurin
      - name: Setup Maven
        uses: stCarolas/setup-maven@v5.1
        with:
          maven-version: 3.9.13
      - name: Build VSC
        run: |
          corepack enable
          pnpm install
          pnpm run build:production
          pnpm run package
      - name: Start VS Code host with extension and MCP
        run: |
          bash .github/workflows/mcp.sh $PWD/playwright/tests/workspaces
      - name: Build with Maven
        run: |
          cd aitest       
          mvn clean verify -ntp
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VSCODE_MCP_URI: http://127.0.0.1:32140/mcp
      - name: Archive test reports
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: test-reports
          retention-days: 5
          path: |
            ci-user-data/logs/**/*
            **/*.log
      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            */target/*-reports/*.xml
            !*/target/*-reports/failsafe-summary.xml
          action_fail: true
      # - name: Install Copilot CLI
      #   run: npm install -g @github/copilot
      # - name: Run Copilot
      #   run: copilot --yolo -p "Summarize the changes in this commit"
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
